# Google Form URLs - Copy to .env and fill in the actual URLs
# These are required for the Apply Now and Enquire buttons to work
# Do NOT include these files in version control

VITE_GOOGLE_FORM_APPLY_URL=
VITE_GOOGLE_FORM_ENQUIRY_URL=
